# dsh-sqlite bundle patch: mounts the plugin into a dsh profile composition.
# Install: `dsh plugin --profile <name> add <pkg-or-dir>`
# Row id must stay unique within the profile's bundle layer.
- insert:
    - id: dsh-sqlite
      name: dsh-sqlite
